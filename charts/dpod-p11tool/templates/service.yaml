apiVersion: v1
kind: Service
metadata:
  name: {{ include "dpod-p11tool.fullname" . }}
  labels:
    {{- include "dpod-p11tool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dpod-p11tool.selectorLabels" . | nindent 4 }}
