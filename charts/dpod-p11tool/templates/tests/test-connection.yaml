apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dpod-p11tool.fullname" . }}-test-connection"
  labels:
    {{- include "dpod-p11tool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dpod-p11tool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
