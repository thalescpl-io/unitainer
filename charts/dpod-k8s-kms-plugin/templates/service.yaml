apiVersion: v1
kind: Service
metadata:
  name: {{ include "dpod-k8s-kms-plugin.fullname" . }}
  labels:
    {{- include "dpod-k8s-kms-plugin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dpod-k8s-kms-plugin.selectorLabels" . | nindent 4 }}
